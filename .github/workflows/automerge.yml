name: Enable Auto-merge and Close Issue

on:
  pull_request_target:
    types: [opened, reopened, synchronize, edited, ready_for_review]
  pull_request:
    types: [closed]

jobs:
  call-workflow:
    uses: Adamantine-guild/.github/.github/workflows/automerge-reusable.yml@main
    secrets: inherit
